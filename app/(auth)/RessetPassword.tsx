import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const RessetPassword = () => {
  return (
    <SafeAreaView>
      <Text>RessetPassword</Text>
    </SafeAreaView>
  );
};

export default RessetPassword;
